import { Builder, By, Capabilities, Key, until, WebDriver } from "selenium-webdriver";
import { BasePage } from "../basePages/basePage";
import { pageObjects } from "../basePages/pageObjects";
const sam = new pageObjects()
const fs = require('fs')